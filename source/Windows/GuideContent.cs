namespace UpsGuardian
{
    /// <summary>
    /// Detailed, platform-neutral guide copy.  The Windows view renders each
    /// pair through Localization.T so the same content can be reused by a
    /// future front end without moving UI concerns into this file.
    /// </summary>
    internal static class GuideContent
    {
        public static readonly string[][] Sections = new string[][]
        {
            new[]
            {
                "首次连接 NUT",
                "程序通过 NUT 读取 UPS 状态，默认端口是 3493。首次使用请在“连接设置”填入 UPS 服务器地址、端口和设备名，保存后等待状态更新。若出现 ACCESS-DENIED，请在 NAS/UPS 服务器的 NUT 设置中，把本机地址加入允许的客户端列表；连接失败还应检查网络连通性、端口和设备名是否正确。不同 NAS 的页面名称可能不同，按服务器文档找到允许客户端的设置即可。本程序只读查询，不会修改 UPS 配置。"
            },
            new[]
            {
                "看懂 UPS 总负载",
                "概览显示的是 UPS 上所有设备的整体负载，不只是本机。服务器能提供实测瓦数时优先显示实测值；否则用 UPS 额定瓦数 × 负载率估算，并标注为估算；没有实测值、又缺少估算所需信息时显示未知。额定输出来自 UPS/服务器信息，不能为了匹配功率计而随意改写。功率计插座可帮助你观察电脑或插座的实际耗电，但它与 UPS 的整体负载口径不同，读数不必相等；本程序也不会降低其他设备的负载。"
            },
            new[]
            {
                "设置阈值与恢复",
                "高负载条件必须严格超过“触发阈值”并持续确认，默认 5 秒后才降本机功耗；等于阈值不会触发。恢复条件是低于“触发阈值 − 恢复回差”，默认持续 30 秒。比如设置 80% 阈值、10% 回差，需超过 80% 持续 5 秒才动作，回落到 70% 以下并保持 30 秒才恢复。回差用于避免边界抖动；阈值应按 UPS 额定输出和实际需求设置，不要随意填超过额定值。"
            },
            new[]
            {
                "CPU 与 GPU 限制",
                "CPU 最大处理器状态是 Windows 电源计划的上限，不等于任务管理器的 CPU 利用率，也不是精确瓦数上限。程序会记录并尝试应用较低的 CPU 上限；GPU 填 0 表示关闭 GPU 控制，只有检测到受支持的本机 GPU 接口才允许设置功率范围。恢复时只恢复本程序拥有且未被外部改动覆盖的限制；冲突或失败会保留恢复记录并提示处理，不保证无条件写回原值。"
            },
            new[]
            {
                "低电量与休眠",
                "低电量保护只在 UPS 正处于电池供电时判断：电量低于阈值或预计续航低于阈值，默认连续确认 5 秒后进入可配置倒计时。倒计时期间来电、条件解除或 UPS 数据过期都会取消休眠；倒计时结束调用 Windows 休眠，并暂停自动保护。它是休眠而非强制关机，系统会尝试保留会话，但应用是否保存文件由应用自己决定。"
            },
            new[]
            {
                "管理员权限与手动启用",
                "本版把限功耗与休眠合并为一个“自动保护”开关，该开关要求管理员身份；读取 UPS、查看负载和普通设置不要求管理员。点击“以管理员身份打开”只会重新启动到提升权限，不会自动开启保护；新配置默认关闭，已保存的状态可能在管理员启动后继续生效，每次启动请以概览显示状态为准。若存在上次恢复记录，请先“暂停并恢复限制”，再手动启用保护。"
            },
            new[]
            {
                "暂停、托盘与退出",
                "“暂停并恢复限制”会关闭新的自动动作；若本程序仍在执行操作，会等操作结束后尝试恢复它拥有且未被外部覆盖的 CPU/GPU 限制。冲突或恢复失败时会保留恢复记录并提示。托盘菜单的“退出并恢复限制”完成同样流程后才真正退出；窗口 X 或“收起到托盘”只隐藏窗口，后台仍在监测。登录自启需要管理员设置，是否启用保护以每次启动的概览状态为准。"
            },
            new[]
            {
                "休眠后的会话边界",
                "休眠会尽量保留 Windows 会话状态，但不等于替所有软件保存文档、检查点或计算进度，也不保证网络连接、远程会话、驱动状态和外部设备恢复。长时间计算任务应使用任务自身的保存/断点功能，重要文件先手动保存。UPS 守护只按条件发起休眠，不会扫描应用并替它完成保存；恢复后先确认供电和任务状态，再按概览决定是否启用保护。"
            },
            new[]
            {
                "断线、陈旧数据与恢复",
                "程序通常每 2 秒轮询一次；短暂断线时暂用最后一份有效数据，不会立刻清除计时。只有当最后数据超过有效期限，策略才清除等待中的触发计时并暂停新的降功耗或休眠动作；已经成功应用的限制会保留。恢复连接后需重新满足确认时间。若发现上次限制未恢复，保护会保持关闭并提示先执行“暂停并恢复限制”；断网不会伪造负载或作出关机判断。"
            },
            new[]
            {
                "版本更新、支持开发与后续平台",
                "版本页可检查 GitHub 发布、查看更新记录并下载在线更新。安装前必须暂停保护并恢复原限制；更新程序会校验下载文件，失败时尝试回滚，并保留 data 目录中的设置、日志与恢复记录。支持开发页的收款码完全自愿，不改变功能。更新完成后请核对版本和连接状态。当前发布目标是 Windows；macOS 适配会在后续阶段继续。"
            }
        };
    }
}
